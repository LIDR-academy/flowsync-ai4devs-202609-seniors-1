{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in */frontend/*) frontend/node_modules/.bin/prettier --write --ignore-unknown \"$f\" ;; esac; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
