# Comparación: con harness vs sin harness

Ticket: FLOW-8 «Implementar login en el frontend» (sin descripción en Jira: solo el título) · 16-09-2026 · Opus 5 x Max · Claude Code en las dos copias.

Misma instrucción, escrita a mano en las dos (la skill `/priority-ticket` no llegó a montarse), lanzada a las 14:54 en cada una:

```
get this ticket from Jira: https://rodlimco.atlassian.net/browse/FLOW-8
draft an action plan
```

Dos cosas que igualan las copias más de lo que el ejercicio supone: el MCP de Atlassian está en la cuenta de Claude Code, no en el repo, así que la copia pelada también leyó el ticket; y las dos sesiones abrieron en modo plan, con el mismo flujo de aprobación. Ningún plan se aplicó.

## Parte A

| | Con harness | Sin harness |
|---|---|---|
| Archivos que propone tocar (contados) | **11**, todos en `frontend/`: 7 nuevos (`src/api/client.ts`, `src/api/types.ts`, `src/features/auth/authAPI.ts`, `useSession.ts`, `useLogin.ts`, `LoginPage.tsx`, `LoginPage.css`) + 4 modificados (`src/App.tsx`, `src/App.css`, `src/index.css`, `index.html`). Backend 0, dependencias nuevas 0. Plan entregado a las 14:59, cinco minutos después del prompt, sin preguntas previas. | PENDIENTE: a las 15:08 (14 min) aún no había plan. Secuencia observada: lee el ticket por el MCP, lanza dos subagentes Explore (backend y frontend, 14:55–14:59) para redescubrir el contrato de la API que `AGENTS.md` ya documenta, escribe un borrador solo con hallazgos (15:00), hace 3 preguntas (15:00) y lanza un subagente Plan (15:02) que a las 15:08 seguía leyendo archivos (49 llamadas a herramientas). |
| Convenciones que respetó (nombradas) | Todas las de `AGENTS.md → Frontend conventions` que el plan toca: `src/features/auth/` para pantalla, hooks y llamadas; `src/api/client.ts` como único cliente HTTP y único lector de `VITE_API_URL`; `src/api/types.ts` para tipos compartidos; sin `utils/`/`helpers/`/`lib/`; CSS junto al componente sobre los tokens de `index.css`; `PascalCase.tsx` con export default con el nombre del archivo, `useThing.ts`, acrónimos en mayúsculas (`authAPI`, `APIError`, `baseURL`); `import type`, sin `enum` ni parameter properties; reemplaza `App.tsx`/`App.css` del starter; sin router, sin cliente HTTP externo, sin Vitest («nothing ahead of it»); contrato de la API respetado (`{ data }` desenvuelto, `response.ok`, 422/400/401 mapeados, `fullName: null`, Bearer solo en rutas protegidas, token borrado en logout y 401); backend intacto; cierre con `/adversarial-review` y `/pr`; commit `type(scope): resumen` en español. Detectó y avisó de que el ticket no tenía descripción y de que el ejercicio pide no aplicar el plan. | PENDIENTE (sin plan final). Lo visible hasta ahora: identificó bien el contrato del backend (rutas, `{ data }`, errores 400/422/401, `fullName: null`) y el estado del frontend (starter intacto, tokens de `index.css`, flags del tsconfig), sin ninguna guía escrita. |
| Convenciones que no respetó (nombradas) | Toca `index.html` (título y `lang`) sin que el ticket lo pida: choca con «files touched that the task didn't need» del adversarial-reviewer. Mete el almacenamiento del token dentro de `src/api/client.ts`, que `AGENTS.md` define como «base URL, JSON, Bearer header, error mapping» (discutible). No registró el prompt en `prompts.md` por su cuenta (ver Parte B.3). | PENDIENTE (sin plan final). |
| Veces que tuve que intervenir | 0 correcciones. El agente no preguntó nada antes de entregar el plan (decidió alcance, tests y router desde `AGENTS.md`, con cada decisión y su alternativa en una tabla). Una pregunta suya después de aprobar el plan: si aplicarlo o parar; paré. | 1 antes de tener plan: tres preguntas a la vez (router sí/no, login solo o con signup, Vitest o verificación manual), las tres con la opción recomendada, que acepté. Son las mismas decisiones que la copia con harness tomó sola porque `AGENTS.md` las responde. |
| Qué me tocaría arreglar a mano antes de enseñárselo a alguien | Poco: sacar `index.html` del alcance si quiero ceñirme al ticket; confirmar el idioma de la UI (asumió español, `AGENTS.md` no lo dice); decidir si restaurar sesión al recargar y logout entran en «login» (el plan lo asume y lo justifica). El plan se puede enseñar tal cual. | PENDIENTE (sin plan final). |

## Parte B

1. Hasta qué pieza llegué, y cuál me costó más de lo que esperaba: no seguí el orden del directo. Monté `AGENTS.md` con `CLAUDE.md` que lo importa (Prompt 1, sin `/init` y en orden inverso al de la lección), el subagente `adversarial-reviewer` con su skill `/adversarial-review` (Prompts 2–4), la skill `/pr` en lugar de `/commit` (Prompt 5) y las reglas de proceso al final de `AGENTS.md`, no de `CLAUDE.md`. El MCP de Atlassian ya estaba en la cuenta. No llegué a `/priority-ticket` ni al hook de Prettier. La que más costó: el adversarial-reviewer, tres prompts hasta dejar la skill en `.agents/skills/` con symlink en `.claude/skills/` para que otras IAs la encuentren.
2. La primera diferencia que vi entre las dos salidas, y en qué me fijé para verla: el tiempo hasta tener un plan y lo que hizo el agente con la incertidumbre. Con harness, plan completo a los cinco minutos y cero preguntas, con las decisiones tomadas desde `AGENTS.md` y anotadas como supuestos; sin harness, cuatro minutos de subagentes redescubriendo el contrato de la API, tres preguntas al usuario y, catorce minutos después, todavía sin plan. Me fijé en los timestamps y la secuencia de llamadas a herramientas de las dos sesiones (`~/.claude/projects/<copia>/*.jsonl`) y en los dos archivos de plan de `~/.claude/plans/`.
3. Algo que dejé escrito en el harness y que el agente no cumplió igualmente: `AGENTS.md → Course rules` dice que cada prompt lanzado se registra tal cual en `prompts.md`, con modelo y herramienta. El agente nunca lo hizo por iniciativa propia: los seis prompts los pegué yo, el «Qué salió» del Prompt 4 se lo tuve que pedir y el del Prompt 6 lo dejó como copia del 5 hasta que cerró la tarea.
