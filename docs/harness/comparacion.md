# Qué archivos tocó
Respeto la decision de no tocar el backend en ambos casos, con y sin harness.
La estructura de ficheros y aplicacion de separation of concerns fue mejor con el harnes.
Ambos ubicaron los test en la misma carpeta que el codigo.
# Qué convenciones del proyecto respetó y cuáles no
La utilizacion del harness cumplio las convenciones del proyecto, de hay la estructura de ficheros
# Cuántas veces tuviste que intervenir
Durant la ejecucion de cada prompt tuve que aprova multitud de accesso a diferentes carpetas y comandos. Con el harness algo menos porque acabo antes
# Qué te tocaría arreglar a mano
En ambos casos deberia corregir la logica. Dedicando mas tiempo al setup del harness, el prompt y un ticket mas limpio podria originar resultados mas cercanos a un codigo desplegable en produccion.